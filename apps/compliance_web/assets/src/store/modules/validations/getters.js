export default {
  validationStatus: state => state.status,
  authorisationServers: state => state.authorisationServers,
  selectedAspsp: state => state.selectedAspsp,
  validationRunId: state => state.validationRunId,
};
